import 'package:flutter/material.dart';

class SoilInfo extends StatelessWidget {
  const SoilInfo({super.key});

  static const String routeName = "soilinfo";

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
