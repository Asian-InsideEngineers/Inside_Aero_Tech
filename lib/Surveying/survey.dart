import 'package:flutter/material.dart';

class Survey extends StatelessWidget {
  const Survey({super.key});

  static const String routeName = "surveying";

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
